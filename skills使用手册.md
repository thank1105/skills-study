# Skills 使用手册

## 可用 Skills 列表

| Skill | 作用 | 文字触发方式 |
|-------|------|------------|
| brainstorming | 创意工作前的需求探索 | `/brainstorming` 或 "帮我头脑风暴一下..." 或 "我想创建一个新功能，帮我想想怎么做" 或 "这个需求怎么实现比较好" |
| find-skill | 搜索和安装新 skills | `/find-skill` 或 "找一个能做X的skill" 或 "我需要一个能处理Y的工具" 或 "有没有skill可以帮我做Z" |
| frontend-design | 创建高质量前端界面 | `/frontend-design` 或 "帮我设计一个网页/组件" 或 "设计一个漂亮的登录页面" 或 "我需要一个现代化的仪表板界面" |
| interaction-design | 设计微交互和动画 | `/interaction-design` 或 "给这个UI加点交互效果" 或 "加上加载动画" 或 "让这个按钮有点击反馈效果" |
| ui-ux-pro-max | UI/UX 设计工具集 | `/ui-ux-pro-max` 或 "帮我优化这个界面的设计" 或 "用什么配色方案比较好" 或 "帮我选择合适的字体" |
| update-config | 配置 Claude Code 设置 | `/update-config` 或 "改一下我的权限/设置" 或 "允许我运行npm命令" 或 "设置一个环境变量" |
| keybindings-help | 自定义键盘快捷键 | `/keybindings-help` 或 "帮我改快捷键" 或 "把Ctrl+S改成Ctrl+Shift+S" 或 "添加一个新的快捷键" |
| verify | 验证代码改动是否有效 | `/verify` 或 "帮我验证这个改动是否工作" 或 "测试一下这个修复是否有效" 或 "运行应用看看改动效果" |
| code-review | 代码审查和优化 | `/code-review` 或 "帮我审查这段代码" 或 "检查一下这段代码有没有问题" 或 "看看有没有可以优化的地方" |
| simplify | 自动应用代码优化 | `/simplify` 或 "简化这段代码" 或 "优化这段代码" 或 "清理一下这个diff" |
| fewer-permission-prompts | 减少权限提示 | `/fewer-permission-prompts` 或 "减少权限提示" 或 "太多权限提示了" 或 "配置常用命令白名单" |
| loop | 定时循环运行命令 | `/loop 5m /command` 或 "每5分钟运行一次..." 或 "每10分钟检查一次部署状态" 或 "持续监控这个任务" |
| claude-api | 构建 Claude API 应用 | `/claude-api` 或 "帮我用Claude API..." 或 "我想用Claude API做一个应用" 或 "优化我的prompt缓存" |
| run | 启动和运行项目 | `/run` 或 "帮我运行这个项目" 或 "启动开发服务器" 或 "看看这个改动在实际应用中的效果" |
| init | 初始化 CLAUDE.md | `/init` 或 "初始化项目文档" 或 "创建项目文档" 或 "生成CLAUDE.md" |
| review | 审查 pull request | `/review` 或 "帮我审查这个PR" 或 "看看这个PR有什么问题" 或 "审查一下这个pull request" |
| security-review | 安全审查 | `/security-review` 或 "做一个安全审查" 或 "检查安全漏洞" 或 "审查一下有没有安全问题" |
